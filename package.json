{
  "name": "bibliography-sorter",
  "version": "1.0.0",
  "description": "Bibliography sorting tool with Word document processing",
  "scripts": {
    "dev": "python -m uvicorn api.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Build complete'",
    "start": "python -m uvicorn api.main:app --host 0.0.0.0 --port 8000"
  },
  "dependencies": {},
  "devDependencies": {}
}